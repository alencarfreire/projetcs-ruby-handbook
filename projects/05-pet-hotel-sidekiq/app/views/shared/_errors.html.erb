<% if record.errors.any? %>
  <div class="errors" role="alert">
    <p>Não foi possível salvar:</p>
    <ul>
      <% record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
